# TikTranslate — AI 分析侧边栏 Design Brief

---

**使用者：** TikTok BD（商务拓展），负责找达人合作推广产品（目前有两个产品：自动猫砂盆、工业水冷扇）

**核心目标：** 分析爆款视频的成功原因 → 结合自己的产品卖点 → 生成一份英文软性建议 → 直接复制发给达人参考拍摄

---

## 整体布局

页面分为左右两栏：
- **左侧：** 视频播放区 + 字幕区(已完成，本次不需要更改)
- **右侧：** AI 分析侧边栏（本次设计范围）

右侧侧边栏顶部有一个标题栏，包含：
- 左边：「✦ AI 分析」标题
- 右边：⚙️ 设置图标（点击进入产品设置页面）+ 「已完成」状态标签

---

## 视觉风格

- 暗色主题（Dark mode）
- 背景色：#111111
- 卡片/区块背景：#1a1a1a
- 边框：1px solid #2a2a2a
- 主色调：#534AB7（紫色）
- 字体：系统默认 sans-serif
- 圆角：8px
- 整体风格参考现有截图，保持一致

---

## Tab 结构

右侧侧边栏包含 **4 个 Tab**，以下逐一说明：

---

### Tab 1：概览

**内容：**

**① 两个数据卡片（并排）**
- 左卡片：综合爆款评分（大字显示，紫色，如 8.7）+ 副标题「高复制价值」
- 右卡片：视频时长（大字显示，如 0:58）+ 副标题「短视频最优区间」

**② 核心卖点**
- 标题：「核心卖点」
- 内容：多个 Tag 标签，横向排列，可换行
- Tag 样式：圆角胶囊形，不同颜色区分（紫/绿/橙/红/蓝）
- 数据来源：Qwen 分析视频后返回

**③ 内容评分**
- 标题：「内容评分」
- 5 个评分维度，每行：维度名称 + 进度条 + 数字
- 维度：说服力 / 钩子强度 / 爆款潜力 / 转化引导 / 视觉演示
- 进度条颜色各不相同（紫/绿/橙/粉/蓝）
- 数据来源：Qwen 分析视频后返回

---

### Tab 2：视频结构

**内容：**

标题：「视频叙事结构拆解」

列表形式，每个结构段包含：
- 序号圆圈（紫色背景白字）
- 段落标题（加粗）+ 时间戳（如 0:00–0:05，灰色小字）
- 描述文字（说明这段做了什么、为什么有效）
- 2-3 个 Tag 标签（标注类型，如「好奇心缺口」「场景代入」）

**示例结构（5段）：**
1. 强钩子开场 0:00–0:05
2. 痛点共鸣 0:05–0:12
3. 产品功能演示 0:12–0:40
4. 信任背书 0:40–0:48
5. 紧迫感收口 0:48–0:58

数据来源：Qwen 分析视频后返回

---

### Tab 3：爆点话术

**内容分为两个区块：**

**① 高效钩子话术提取**
- 标题：「高效钩子话术提取」
- 列表形式，每条包含：
  - 时间戳（如 0:00，灰色小字）
  - 原文（视频原语言，如西语/英语）
  - 中文翻译（加粗）
  - 一个 Tag 标签标注钩子类型（如「⚡ 开场钩子 — 好奇心缺口」）
- 卡片之间有分隔

**② 可复用话术模板**
- 标题：「可复用话术模板」
- 4 个模板卡片：开场模板 / 演示模板 / 结果模板 / 收口模板
- 每个卡片：左上角 Tag 标注类型 + 模板文字（带 [填空] 占位符）

数据来源：Qwen 分析视频后返回

---

### Tab 4：达人建议（核心功能）

**这是最重要的 Tab，是最终输出给达人的内容。**

**页面结构：**

**① 产品选择下拉框**
- Label：「Select product」
- 下拉选项：用户在设置页面中添加的产品列表
- 默认选中第一个产品
- 样式：暗色风格，与整体一致

**② 生成按钮**
- 文字：「Generate creator suggestion」
- 样式：紫色背景（#534AB7），白色文字，全宽，圆角
- 点击后进入 loading 状态，文字变为「Generating...」
- loading 时按钮 disabled

**③ 生成结果区域**（生成后显示）
- 外层：暗色卡片，带边框
- 顶部栏：左边「Ready to send」小标签，右边「Copy」按钮
- 内容：AI 生成的英文软性建议文字
  - 风格：友好、温暖、尊重达人创作空间
  - 格式：开头夸奖达人，中间 4-5 条建议（数字编号），结尾鼓励
  - 字体大小：14px，行高 1.8
- 「Copy」按钮点击后变为「✓ Copied」，2秒后恢复

**生成逻辑（供开发参考）：**
```
prompt = 视频分析结果（钩子/演示/情感/CTA）
       + 选中产品的卖点信息（名称/目标人群/卖点/场景）
       → 发给 Claude API
       → 返回英文软性建议文字
```

---

## 产品设置页面（⚙️ 进入）

点击右上角 ⚙️ 图标后，侧边栏切换为产品设置页面。

**页面结构：**

**顶部：**
- 标题「Product Settings」
- 右边「✕ Close」按钮，点击返回 AI 分析

**产品列表：**
- 每个产品一个卡片
- 展示模式：产品名称（加粗）+ Edit 按钮 + 以下字段的只读展示：
  - Audience（目标人群）
  - Selling points（核心卖点）
  - Scene（使用场景）

**编辑模式（点击 Edit 后展开）：**
- 4 个输入框/文本域（内联展开，不跳转页面）：
  - Product name（单行输入）
  - Target audience（多行，2行高）
  - Core selling points（多行，3行高，逗号分隔）
  - Key usage scenes（多行，2行高）
- 底部：Save 按钮（紫色）+ Cancel 按钮

**预设两个产品（初始数据）：**

产品一：
- Name: Automatic Cat Litter Box
- Audience: Cat owners with large cats or multiple cats
- Selling points: 106L interior space, fits cats up to 25lbs, app control, auto-clean, safety sensors, quiet motor
- Scene: Daily cat care, multi-cat households, busy pet owners

产品二：
- Name: Industrial Evaporative Cooler
- Audience: Outdoor enthusiasts, families with backyard, no AC households
- Selling points: No AC needed, works outdoors, whisper-quiet, covers large area, energy efficient
- Scene: BBQ, backyard hangout, garage, patio, summer outdoor activities

---

## 数据流说明

```
用户粘贴 TikTok 链接
    ↓
后端获取视频 → Qwen3.7-Plus 分析视频
    ↓
后端返回 JSON（包含以下字段）：
{
  sellingPoints: [],      // 核心卖点标签
  scores: {               // 内容评分
    persuasion: 8.7,
    hookStrength: 9.2,
    viralPotential: 8.1,
    conversionGuide: 8.8,
    visualDemo: 9.0
  },
  videoStructure: [],     // 视频结构段落
  hooks: [],              // 爆点话术
  summary: ""             // 话术摘要
}
    ↓
前端展示在 Tab 1/2/3
    ↓
用户在 Tab 4 选择产品
    ↓
前端把「JSON分析结果 + 产品卖点」拼成 prompt
    ↓
调用 Claude API 生成英文软性建议
    ↓
用户一键复制发给达人
```

---

## 交互细节

- Tab 切换：点击即切换，无动画
- 设置页面：覆盖整个侧边栏区域，不是弹窗
- 产品编辑：内联展开，不跳转页面
- 生成按钮：loading 期间禁用，防止重复点击
- Copy 按钮：复制成功后 2 秒内显示「✓ Copied」
- 所有文字输入：暗色背景 #1a1a1a，白色文字，边框 #444

---

## 不需要做的东西

- 不需要付费达人脚本功能
- 不需要多用户/权限系统
- 不需要视频投流分析
- 不需要达人匹配功能
- Tab 1/2/3 的数据全部由后端 Qwen 分析返回，前端只负责展示

---

## 交付物要求

- React 组件（.jsx）
- 暗色主题
- 与现有 TikTranslate 界面风格一致
- 产品设置数据暂时存在前端 state，后续再接后端存储
